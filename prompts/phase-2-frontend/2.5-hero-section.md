# 2.5 — Public catalog hero section

## Goal
Implement the supplied desktop hero reference above the existing course grid.
This is only the hero portion of roadmap task 2.5, explicitly requested by the user.

## Instructions and skills read
- `AGENTS.md`, `docs/roadmap.md`.
- `docs/system_design/frontend.md`, `docs/system_design/backend.md`.
- `DESIGN.md`, `src/globals.css`.
- `/Users/macbookpro/.codex/skills/.system/imagegen/SKILL.md`: use the built-in image generator for the reference-inspired raster illustration; save the final asset into this project.

## Inspected code and configuration
- `src/sections/herosection.tsx` and `coursesection.tsx` are empty.
- `src/App.tsx` currently renders the mock course grid directly.
- `src/components/ui/button.tsx` exposes Button and buttonVariants.
- `src/main.tsx` imports globals.css and wraps App in ThemeProvider.
- `src/globle.css` only imports globals.css.
- `package.json`, `vite.config.ts`, and `index.html`: root-level React/TypeScript/Vite, Tailwind 4, Lucide, existing Google font links, lint/build/dev scripts.
- Available image assets are only starter logos; no matching hero illustration exists.

## Decisions and assumptions
- Work in the established root `src/` layout; no restructuring or dependencies.
- Treat the red rectangles as reference annotations, not UI borders.
- Match the reference's bold Inter headline (a local exception to the documented Playfair display rule), desktop two-column composition, green emphasis, pale mint backdrop, badge, and CTA. Use standard theme and sizing utilities, with responsive text sizes up to text-6xl to match the reference.
- On smaller screens stack copy above the illustration and prevent overflow down to 320px.
- Explore Courses is an accessible native anchor to `#courses`, using existing button styling.
- Generate a separate decorative laptop illustration inspired by the supplied reference. A generated asset can approximate the artwork; exact pixel identity requires the original artwork.
- Use semantic surface/text tokens and dark variants for readable dark mode.

## Expected files
- `src/sections/herosection.tsx`: presentational HeroSection.
- `src/assets/hero-learning.png`: generated illustration, or equivalent supported raster extension.
- `src/App.tsx`: render hero, keep grid below, add courses anchor target.
- `docs/roadmap.md`: mark a hero subtask complete under 2.5 after successful checks; leave full catalog task open.
- This prompt: record final asset prompt and verification results.

## Requirements
1. Render badge `INTELLIGENT LEARNING` with a decorative sparkle.
2. Render one h1: `Learn in a smarter, faster way.` Highlight `smarter,` and `faster` in green; reproduce desktop line wrapping where space permits.
3. Render exact supporting copy: `Vibe Learn helps you build real skills with structured courses, hands-on practice and personalized learning paths.`
4. Render `Explore Courses` with a right arrow, visible focus state, and minimum 44px touch target.
5. Illustration: mint/emerald 3D laptop with graduation cap on screen, floating play tile, code tiles, note tile, soft organic backdrop, and small sparkles. No text, reference annotations, or baked-in CTA. Prefer transparent background to blend with both themes.
6. Keep icons and illustration decorative for screen readers. Reserve image space to avoid layout shift.
7. Use existing tokens, standard spacing/radii/shadows, and installed packages only. Keep the component small.

## Security and scope
Static presentation only: no network data calls, credentials, database access, auth changes, or new backend work. Preserve existing course data and cards.

## Acceptance criteria
- Hero appears above the current course cards and follows the supplied composition and copy.
- CTA navigates to the course grid by keyboard and pointer.
- Desktop and mobile layouts have no clipped content or horizontal overflow.
- Both themes have readable text and a coherent background.
- Lint and production build pass; development server starts.
- Only the completed hero subtask is checked in the roadmap.

## Checks
Run from the repository root (the actual frontend workspace):
1. `npm run lint`.
2. `npm run build`.
3. `npm run dev -- --host 127.0.0.1`; verify the page responds at the reported URL.
No backend exists in this task's scope; backend checks and migrations are not applicable.

## Manual test steps
1. Open the dev server URL at a desktop viewport near the supplied 1134px-wide reference; compare copy, columns, green emphasis, spacing, and illustration.
2. Resize to 768px, 375px, and 320px; confirm stacked content, readable wrapping, and no horizontal scroll.
3. Tab to Explore Courses; confirm focus is visible, then press Enter and verify navigation to the course grid.
4. Repeat with a pointer click.
5. Toggle the root dark class in browser developer tools; check text, badge, CTA, and illustration against the dark surface.
6. Check the browser console and network panel for runtime errors and missing hero assets.

## Approval
Approved by the user with `yes` on 2026-09-19. Implemented.

## Implementation and verification results
- Implemented `HeroSection` above the existing grid; CTA links to `#courses`.
- Generated with the built-in image tool and saved to `src/assets/hero-learning.png` (1402 × 1122, transparent PNG).
- Final image prompt: Use case: stylized-concept. Create a standalone website hero illustration, landscape 5:4 composition, transparent background. Soft polished mint and emerald green 3D educational illustration matching the reference in this conversation: open laptop viewed slightly from above, screen upright slanting slightly right, white/mint screen with a solid emerald graduation cap centered. Mint white keyboard base projects toward lower left, emerald keyboard area. Floating rounded green tile with white play triangle upper left; green code tile with white angle brackets middle left; white note tile with three mint horizontal strokes upper right; small white code tile bottom right. Subtle translucent mint organic shapes behind, a sweeping thin emerald arc around lower left and tiny green four-point sparkles. Soft diffuse lighting, rounded corners, delicate dimensional shadows, airy clean composition filling canvas with small margins. No words, no letters, no webpage UI, no badge, no headline, no red boxes or borders. Preserve actual transparent alpha outside the illustration. This asset will be used on both light and dark website backgrounds. Return a saved local image asset.
- `npm run lint`: exit 0; zero errors, one existing react-refresh warning in `src/components/ui/button.tsx:57`.
- `npm run build`: exit 0; 1907 modules transformed. Existing Vite warning about `__dirname` compatibility with a future native config loader.
- Dev server reported ready. Sandbox blocked a subsequent localhost binding; verification outside the sandbox returned HTTP 200 for both `/` and `/src/assets/hero-learning.png`.
- Illustration visually inspected. No browser automation tool available; responsive rendering, keyboard interaction, and dark-mode appearance still require the manual checks above. The existing `D` keyboard shortcut toggles themes.
- Full catalog task remains open; only its hero subtask is complete.
