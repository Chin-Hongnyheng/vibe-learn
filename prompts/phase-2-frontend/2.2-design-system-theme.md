# Implementation Prompt: 2.2 Implement Global Design System Theme in globals.css

## 1. Goal
Implement the Vibe Learn design system theme defined in `DESIGN.md` as shadcn-compatible CSS variables and Tailwind CSS v4 `@theme inline` configuration inside `src/globals.css` (and integrate with the frontend project).

---

## 2. Skills Read
- `using-superpowers`: Workflow instructions for skill usage and disciplined execution.
- `context7`: Retrieved official `@shadcn-ui/ui` documentation on Tailwind CSS v4 theming, CSS variables mapping, and custom token registration via `@theme inline`.

---

## 3. Code Inspected
- `DESIGN.md`:
  - **Primary Palette (Emerald):** Primary 500 (`#10B981`), 400 (`#34D399`), 300 (`#6EE7B7`), 200 (`#A7F3D0`), 100 (`#D1FAE5`).
  - **Neutral Palette (Slate):** Neutral 900 (`#0F172A`), 700 (`#334155`), 500 (`#64748B`), 300 (`#CBD5E1`), 200 (`#E2E8F0`), 100 (`#F1F5F9`), 50 (`#FAFAFC`), White (`#FFFFFF`).
  - **Typography:** Playfair Display (Display 1 & 2), Inter (Headings 1–3, Body Large, Body, Small).
  - **Radius Scale:** xs (4px), sm (8px), md (12px - default), lg (16px), xl (24px), full (9999px).
  - **Elevation Shadows:** sm, md, lg, xl.
- `src/index.css`: Currently configured with Tailwind CSS v4 and default shadcn tokens (violet primary and standard slate).
- `components.json`: Configured with `"css": "src/index.css"`.
- `src/main.tsx`: Currently imports `./index.css`.
- `index.html`: Base HTML template, ready for Google Fonts links (Inter and Playfair Display).

---

## 4. Decisions and Assumptions
- **Workspace Location:** The Vite React app is currently located at the workspace root (`/Users/lyyeakkhai/workspace/vibe-learn/`), which serves as the frontend workspace.
- **File Structure:** We will create `src/globals.css` (or `src/globle.css`) containing the complete shadcn theme tokens and Tailwind v4 theme definitions. To maintain full compatibility with Vite, shadcn CLI (`components.json`), and `main.tsx`, we will wire `src/globals.css` into `src/index.css` (or update `src/main.tsx` and `components.json` to reference `src/globals.css`).
- **Color Token Mapping for shadcn:**
  - *Light Mode (`:root`):*
    - `--background`: `#FAFAFC` (Neutral 50)
    - `--foreground`: `#0F172A` (Neutral 900)
    - `--card`: `#FFFFFF` (White)
    - `--card-foreground`: `#0F172A` (Neutral 900)
    - `--popover`: `#FFFFFF` (White)
    - `--popover-foreground`: `#0F172A` (Neutral 900)
    - `--primary`: `#10B981` (Primary 500)
    - `--primary-foreground`: `#FFFFFF` (White)
    - `--secondary`: `#F1F5F9` (Neutral 100)
    - `--secondary-foreground`: `#0F172A` (Neutral 900)
    - `--muted`: `#F1F5F9` (Neutral 100)
    - `--muted-foreground`: `#64748B` (Neutral 500)
    - `--accent`: `#D1FAE5` (Primary 100)
    - `--accent-foreground`: `#065F46` (Primary 800)
    - `--destructive`: `#EF4444`
    - `--destructive-foreground`: `#FFFFFF`
    - `--border`: `#E2E8F0` (Neutral 200)
    - `--input`: `#E2E8F0` (Neutral 200)
    - `--ring`: `#10B981` (Primary 500)
    - `--radius`: `0.75rem` (12px, matching DESIGN.md's `md` 12px standard)
  - *Dark Mode (`.dark`):*
    - Complementary dark slate/emerald values with high-contrast WCAG AA compliance.
- **Palette & Typography Registration in `@theme inline`:**
  - Register `--color-primary-*` (50–900) and `--color-neutral-*` (50–900) so classes like `bg-primary-500` and `text-neutral-700` work everywhere.
  - Register `--font-sans: 'Inter', sans-serif;` and `--font-serif: 'Playfair Display', serif;` and `--font-display: 'Playfair Display', serif;`.
- **Google Fonts:** Include Google Fonts (`Inter` and `Playfair Display`) in `index.html` via standard `<link>` tags, without adding any new npm dependencies (satisfying the Technology Lock rule).

---

## 5. Files Expected to Touch
- `src/globals.css` (Create: complete shadcn theme tokens and Tailwind v4 inline theme)
- `src/index.css` (Update: import or re-export `globals.css` to preserve existing references)
- `index.html` (Update: add Google Fonts preconnect and stylesheet links for Inter and Playfair Display)
- `components.json` (Update: point `"css": "src/globals.css"`)
- `src/main.tsx` (Update: import `./globals.css`)

---

## 6. Requirements
- All color values, font families, radii, and shadows from `DESIGN.md` must be faithfully represented.
- Both shadcn UI component semantic utility classes (`bg-primary`, `text-primary-foreground`, `bg-background`, `border-border`) and DESIGN.md palette classes (`bg-primary-500`, `text-neutral-900`) must be fully functional.
- Zero new npm packages installed (Technology Lock rule).

---

## 7. Security Considerations
- Styling only; no secret keys, authentication tokens, or sensitive data are touched.

---

## 8. Acceptance Criteria
- `src/globals.css` contains the complete theme conforming to shadcn UI and Tailwind CSS v4.
- Light mode and dark mode styles match `DESIGN.md`.
- Typography is configured for Inter and Playfair Display.
- `npm run lint` passes with 0 errors.
- `npm run build` passes with 0 errors.

---

## 9. Checks to Run
- `npm run lint`
- `npm run build`

---

## 10. Manual Test Steps
1. Verify `src/globals.css` contains all `:root`, `.dark`, and `@theme inline` variables.
2. Run `npm run build` and ensure TypeScript and Vite build succeed without CSS or syntax warnings.
3. Start the dev server with `npm run dev` and verify that the page renders using the new emerald and slate theme tokens.
