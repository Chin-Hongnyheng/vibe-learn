# Implementation Prompt: 2.5 Showcase Section ("Why Vibe Learn?")

## 1. Goal
Build the "Why Vibe Learn?" Showcase section matching the provided UI reference desktop image and design tokens from `DESIGN.md`, breaking it down into modular, reusable components and structuring the section inside `src/sections/showcasesection.tsx`.

---

## 2. Skills Read
- `using-superpowers`: Disciplined workflow execution and step-by-step verification.
- `DESIGN.md` & `AGENTS.md`: Strict UI reproduction, semantic token compliance, typography rules, dark mode support, and no arbitrary hex values or sizes.

---

## 3. Code Inspected
- `AGENTS.md`: Strict loop, UI reproduction guidelines, token rules, theme consistency, and responsive adaptation.
- Reference Image:
  - Outer container: Large rounded card (`rounded-3xl` / `rounded-2xl`) with soft mint-green background (`bg-primary-50/40` or `bg-emerald-50/50`, `border border-primary-100 dark:border-neutral-800`).
  - Left column:
    - Eyebrow: "WHY VIBE LEARN?" (`text-primary-600 font-bold tracking-wider uppercase text-xs sm:text-sm`).
    - Title: "Build the skills that matter." (`text-3xl md:text-4xl font-extrabold text-neutral-900 tracking-tight leading-tight`).
    - Paragraph: "More than just courses — Vibe Learn gives you the tools, support, and structure to grow your career and achieve your goals." (`text-neutral-500 leading-relaxed text-sm md:text-base`).
  - Dividers: Subtle vertical divider borders separating columns on desktop (`border-primary-100 dark:border-neutral-800`).
  - Middle column:
    - Feature 1: Brain icon in circular mint badge (`Brain`), "Expert-Led Content", "Learn from industry professionals with real-world experience."
    - Feature 2: Route / nodes icon in circular mint badge (`Route` / `Workflow`), "Structured Learning Paths", "Follow clear paths from beginner to advanced."
  - Right column:
    - Feature 3: Lightning bolt icon in circular mint badge (`Zap`), "Hands-On Practice", "Build real projects and apply what you learn immediately."
    - Feature 4: Users icon in circular mint badge (`Users`), "Lifetime Access", "Learn at your own pace, anytime, anywhere."
- `src/sections/showcasesection.tsx`: Currently empty (0 bytes).
- `src/components/ui/` & `src/components/common/`: Common component directory structure.

---

## 4. Decisions and Assumptions
- **Modular Component Breakdown:**
  1. `src/components/showcase/showcase-intro.tsx`: Handles the left column with the eyebrow badge, title, and descriptive copy.
  2. `src/components/showcase/showcase-feature-item.tsx`: Reusable presentation component for individual showcase features (circular icon badge, title, description).
  3. `src/sections/showcasesection.tsx`: Main showcase section composing `ShowcaseIntro` and `ShowcaseFeatureItem` with the background card container, grid columns, and responsive vertical/horizontal dividers.
- **Icon Library:** Use icons from `lucide-react` (already installed in `package.json`): `Brain`, `Route` (or `Workflow`), `Zap`, `Users`.
- **Styling Tokens:**
  - Background: `bg-primary-50/50 dark:bg-neutral-900/60`
  - Border: `border-primary-100 dark:border-neutral-800`
  - Text: `text-primary-600 dark:text-primary-400` (eyebrow/icons), `text-neutral-900 dark:text-white` (headings), `text-neutral-500 dark:text-neutral-400` (copy)
  - Typography: `font-sans` with standard weights (`font-bold`, `font-extrabold`) per `DESIGN.md`.
- **Responsiveness:**
  - Desktop (`lg`): 3 columns separated by subtle vertical border lines as depicted in the reference image.
  - Mobile/Tablet: Stacked layout with intuitive spacing and subtle dividers.

---

## 5. Files Expected to Touch
- `src/components/showcase/showcase-intro.tsx` (NEW: Intro headline & paragraph)
- `src/components/showcase/showcase-feature-item.tsx` (NEW: Feature card with icon badge)
- `src/sections/showcasesection.tsx` (UPDATE: Main showcase section container)

---

## 6. Requirements
- Exact 1:1 reproduction of the desktop design in the reference image.
- Component-driven architecture with clean, reusable sub-components.
- Responsive design adapting gracefully to tablet and mobile viewports.
- Fully compatible with both light and dark mode using theme tokens.
- No new npm packages installed (Technology Lock rule).

---

## 7. Security Considerations
- Purely presentation components; no user input or sensitive data handling.

---

## 8. Acceptance Criteria
- Showcase section visually mirrors the provided reference desktop screenshot.
- Individual sub-components (`ShowcaseIntro`, `ShowcaseFeatureItem`) are clean, typed, and well-structured.
- `npm run lint` passes without errors.
- `npm run typecheck` passes without errors.

---

## 9. Checks to Run
- `npm run lint`
- `npm run typecheck`

---

## 10. Manual Test Steps
1. Verify `src/sections/showcasesection.tsx` renders all 4 features and intro copy properly.
2. Verify responsive layout on desktop, tablet, and mobile.
3. Test dark mode toggle to confirm high-contrast readability.
